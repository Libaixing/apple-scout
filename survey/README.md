# 数据源调查 (Survey)

## 原则
- 只调查，不开发。在确认入口可用前，不写任何解析逻辑。
- 每次只调查一个入口，通过后再扩展。

## 成功标准
一个入口通过调查需满足：
- [ ] 连续3天 GitHub Actions 请求成功 (HTTP 200)
- [ ] 无验证码/风控拦截
- [ ] 返回内容包含可识别的商品数据
- [ ] 页面结构或接口字段稳定
- [ ] 响应时间 ≤ 5 秒

## 结果记录
每次运行自动追加到 `results.csv`，格式：
`时间, 数据源, HTTP状态码, Content-Type, 响应大小, 是否成功, 备注`
